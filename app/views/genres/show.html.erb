
<h1> <%= @genre.name %> </h1>

<ul>
    <% @genre.songs.each do |s| %>
        <li> <%= link_to s.name, song_path(s) %> </li>
    <% end %>
</ul>

<%= link_to "Edit this Genre", edit_genre_path %> <br>

<%= button_to "Delete Genre", genre_path(@genre), method: :delete %>
<br>

<%= link_to "All Genres", genres_path %>
